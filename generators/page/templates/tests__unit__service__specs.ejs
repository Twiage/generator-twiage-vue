import <%= name %>Service from "@/<%= name %>/service/<%= name %>.service";

describe("<%= name %> Service", () => {
  it("has a failing test to start TDD", () => {
    expect(<%= name %>Service).toBe(true);
  });
});
